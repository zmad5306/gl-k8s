apiVersion: apps/v1
kind: Deployment
metadata:
  name: gl-api-deployment
  labels:
    app: gl-api
spec:
  replicas: 1
  minReadySeconds: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 1
      maxSurge: 1
  selector:
    matchLabels:
      app: gl-api
  template:
    metadata:
      labels:
        app: gl-api
    spec:
      containers:
      - name: gl-api
        image: zmad5306/gl-api:latest
        ports:
        - containerPort: 8080